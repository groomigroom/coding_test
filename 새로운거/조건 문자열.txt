if (ineq === ">" && eq === "=") {
        if (n >= m) {
            answer = 1;
        } else {
            answer = 0;
        }
    } else if (ineq === "<" && eq === "=") {
        if (n <= m) {
            answer = 1;
        } else {
            answer = 0;
        }
    } else if (ineq === ">" && eq === "!") {
        if (n > m) {
            answer = 1;
        } else {
            answer = 0;
        }
    }else if (ineq === "<" && eq === "!") {
        if (n < m) {
            answer = 1;
        } else {
            answer = 0;
        }
    }
